{
  "name": "steelart_server",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "pnpm typecheck",
    "lint": "node --eval \"console.log('lint step will be added in a later stage')\"",
    "test": "node --eval \"console.log('test step will be added in a later stage')\"",
    "sam:validate": "node --eval \"console.log('SAM validation will be wired in a later stage')\"",
    "sam:build": "node --eval \"console.log('SAM build will be wired in a later stage')\""
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0"
  }
}
