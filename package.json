{
  "name": "steelart_server",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "pnpm typecheck",
    "lint": "node --eval \"console.log('lint step will be added in a later stage')\"",
    "test": "tsx --test tests/unit/**/*.test.ts",
    "sam:validate": "sam validate --template-file infra/sam/template.yaml --config-file samconfig.toml --config-env default",
    "sam:build": "sam build --template-file infra/sam/template.yaml --config-file samconfig.toml --config-env default",
    "sam:deploy:guided": "sam deploy --guided --template-file infra/sam/template.yaml --config-file samconfig.toml --config-env dev"
  },
  "dependencies": {
    "mysql2": "^3.15.1",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.0.0"
  }
}
